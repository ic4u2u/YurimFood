# Yulim Food ERP Backend Environment Variables

PORT=5000

# PostgreSQL Database Connection String (uncomment and replace when Cloud SQL is provisioned)
# DATABASE_URL=postgresql://<db_user>:<db_password>@<db_host>:<db_port>/<db_name>
